# 乐顺科纺小程序 — 工作报表

> 日期：2026年5月12日  
> 项目：乐顺科纺 微信小程序

---

## 一、今日完成事项

### 🎬 首页 Hero 区：图片轮播 → 全屏视频

| 模块 | 内容 |
|------|------|
| **视频替换** | 删除全部4场景图片轮播逻辑，改为 `<video>` 组件（autoplay/loop/muted/object-fit:cover） |
| **高度调整** | 容器高度最终定为 `90vh`，自然露出下方内容引导滑动 |
| **暗角效果** | 四周 `radial-gradient` 椭圆渐变遮罩，增强电影质感 |
| **删除内容** | heroScenes / heroPhase / 触摸手势 / 圆点指示器 / 品牌文字层全部清理 |

### ✨ 视频结尾 Logo 动画

| 模块 | 内容 |
|------|------|
| **触发方式** | `bindtimeupdate` 检测最后5秒（`currentTime >= duration - 5`） |
| **动画样式** | `opacity 0→1` + `translateY 36rpx→0`，无 scale（防模糊） |
| **循环处理** | 视频重头（`currentTime < 1`）自动淡出，不暂停 |
| **图片压缩** | `logo-divider.png` 用 TinyPNG 从 481KB → 18KB |

### 🔧 面料详情页重构

| 模块 | 内容 |
|------|------|
| **删除轮播** | 移除顶部 720rpx 沉浸式图片轮播、计数器、渐变遮罩 |
| **信息卡置顶** | 品名/英文名/品类标签/描述卡片移至顶部，`navbarHeight` px 动态占位 |
| **默认视角** | 所有入口默认「色卡视角」（原为产品视角） |
| **切换顺序** | 色卡在左，产品在右 |
| **导航栏** | 去掉滚动透明逻辑，始终实色 |
| **代码清理** | 移除 `currentImageIndex` / `images` / `onSwiperChange` / `onPageScroll` |

### 🎨 产品视角 UI 去 emoji 高级化

| 区块 | 改前 | 改后 |
|------|------|------|
| 面料亮点 | 2×2 方格 + emoji 图标 | 竖向编号列表（01/02/03/04）+ 细分隔线 |
| 适用场景 | 彩色方块 + emoji + 横滑 scroll-view | 简洁胶囊文字标签（flex-wrap） |
| 洗护指南 | emoji 图标 + 灰色背景块 | 左侧金色渐变细竖线 + 标题描述 |

### 🗂️ Obsidian 知识库整理

| 文件 | 操作 |
|------|------|
| `踩坑记录.md` | 删除重复 P6，补录今日 P9～P13 共5个新 Bug |
| `问题记录.md` | 停止维护，改为指向踩坑记录的跳转说明 |
| `.windsurfrules` | 新增规则#6「收工同步协议」，定义触发词和6步自动同步流程 |

---

## 二、Bug 修复

| #   | 现象             | 根因                                   | 修复方案                               |
| --- | -------------- | ------------------------------------ | ---------------------------------- |
| P9  | tabBar 从面料页消失  | `goSearch()` 用 `navigateTo` 打开 tab 页 | 改为 `switchTab`                     |
| P10 | 首页全白屏          | `lazyCodeLoading` 开启但组件未在各页 json 登记  | 删掉该配置项                             |
| P11 | Logo 动画在视频开头出现 | `loop + bindended` 触发时机不可靠           | 改用 `bindtimeupdate` 检测最后5秒         |
| P12 | Logo 图片模糊      | CSS `scale` 动画 GPU 低分辨率渲染            | 去掉 scale，只用 `opacity + translateY` |
| P13 | console 常驻报错   | Observer 监听已删除的 `.stats-bar` 节点      | 清理监听注册                             |

---

## 三、遗留问题

| 优先级 | 问题 | 说明 |
|--------|------|------|
| 🔴 高 | 面料实拍图 | 当前面料卡片仍为 CSS 渐变占位，需替换为真实面料图 |
| 🔴 高 | 询价表单写库 | `contact.js` 提交只弹窗，数据未写入云数据库 |
| 🟡 中 | 分类页搜索逻辑 | UI 已有，`loadFabrics` 未按 searchKey 过滤 |
| 🟡 中 | 真实地址 | 联系页 `address` 字段仍为"待更新" |

---

## 四、下一步计划

1. 修改TVC视频里的面料服装
2. 询价表单对接云函数 `submitInquiry` → 写入 `inquiries` 集合
3. 分类页搜索 filter 逻辑补全（一行代码）
4. 上线前适配测试（iOS 安全区 / Android 刘海屏）
5. 提交微信审核
