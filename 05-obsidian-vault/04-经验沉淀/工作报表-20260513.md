# 乐顺科纺小程序 — 工作报表

> 日期：2026年5月13日  
> 项目：乐顺科纺 微信小程序

---

## 一、今日完成事项

### 🔗 联系页转化优化

| 模块 | 内容 |
|------|------|
| **详情页底栏合并** | 删除"联系客服"按钮，仅保留全宽"立即询价"金色渐变按钮，聚焦转化路径 |
| **样式清理** | 同步删除 `detail.wxss` 中 `.btn-contact` 无用样式 |

### 📋 电子营业执照

| 模块 | 内容 |
|------|------|
| **首页底部改版** | 删除"开启合作"CTA卡片，替换为"电子营业执照 + 公司名 + 品牌logo"信息底栏 |
| **专属证照页** | 新建 `pages/license/license`（米白主题，自定义导航栏+返回按钮） |
| **云存储接入** | 营业执照上传至云存储，`getTempFileURL` 获取临时链接展示，支持长按保存 |
| **图片边缘裁切** | CSS负margin裁掉图片上下深色边缘，无需重新上传图片 |

### 🎬 Splash 启动动画

| 模块 | 内容 |
|------|------|
| **新建Splash页** | `pages/splash/splash` 设为 `app.json` 第一个页面 |
| **动画效果** | logo渐入（0.1s）→ 停留 → 渐出（1.5s）→ 自动跳首页（2.05s） |
| **跳转修复** | 发现白屏Bug，由 `redirectTo` 改为 `switchTab`（tabBar页专用） |

### ✨ 全局跳转 Loading 动画

| 模块 | 内容 |
|------|------|
| **新建组件** | `components/loading-mask`，全局注册于 `app.json` |
| **覆盖范围** | 6个页面（index/category/swatch/about/contact/detail）均已接入 |
| **动画效果** | 米白遮罩 + logo opacity 0.12→0.75，350ms，z-index 9999 |
| **重复修复** | 首页用 `_firstShow` 标记跳过首次触发，避免与Splash动画叠加 |

---

## 二、Bug 修复

| # | 现象 | 根因 | 修复方案 |
|---|------|------|----------|
| P14 | Splash跳首页白屏 | `wx.redirectTo` 无法跳转 tabBar 页 | 改为 `wx.switchTab` |
| P15 | 启动动画重复播放1-2帧 | splash结束后首页 `onShow` 立即触发loading-mask | `_firstShow` 实例标记，首次跳过 |

---

## 三、遗留问题

| 优先级 | 问题 | 说明 |
|--------|------|------|
| 🟡 中 | 公司全称待核实 | 营业执照底栏显示"广州市乐顺科纺有限公司"，需老板确认全称 |
| 🟡 中 | 面料数据分散 | 各页面各存一份数据，建议后续统一到 `data/fabrics.js` |
| 🟡 中 | 分类页搜索逻辑 | `loadFabrics` 未按 searchKey 过滤，一行代码可修 |
| 🟢 低 | 联系页真实地址 | `address` 字段仍为"广东省广州市（待更新具体地址）" |

---

## 四、下一步计划

1. 核实营业执照公司全称并更新底栏文字
2. 真实面料数据统一接入（建议 `data/fabrics.js` 或云数据库）
3. 上线前适配测试（iOS 安全区 / Android 刘海屏）
4. 提交微信审核上线
